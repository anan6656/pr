# PR Profile 判断与对比公式说明

## 文档用途

本文说明 PR Analyzer 中 Profile 的构建、对齐、平均、Baseline 对比和形貌判断方法。Profile 指柱体轮廓随相对高度变化的曲线，主要用于回答三个问题：

1. 柱子在相同高度位置的 CD 是否变宽或变窄；
2. 变化发生在顶部、中部还是底部；
3. 变化属于 T-top、Top-rounding、中间细、中间粗、Footing，还是主要表现为 LER/LWR 粗糙度变化。

---

## 一、Profile 的基本数据

| 符号 | 含义 | 单位 |
|---|---|---|
| `j` | 柱子编号 | — |
| `M` | 参与平均的有效柱数量 | 根 |
| `y` | 原始纵向坐标 | px 或 nm |
| `Hj` | 第 j 根柱的高度 | nm |
| `Lⱼ(y)` | 第 j 根柱左边缘 | px 或 nm |
| `Rⱼ(y)` | 第 j 根柱右边缘 | px 或 nm |
| `Wⱼ(y)` | 第 j 根柱的 CD | nm |
| `z` | 归一化高度 | 0–1 |
| `k` | 统一 Profile 采样点编号 | 0–400 |
| `Ȳk` | 平均 Profile 的实际高度坐标 | nm |
| `CD̄k` | 平均 Profile 的 CD | nm |
| `σL,k` | 左边缘柱间标准差 | nm |
| `σR,k` | 右边缘柱间标准差 | nm |

每根柱的 CD 定义为：

```text
Wⱼ(y) = Rⱼ(y) − Lⱼ(y)
```

程序不使用“宽度加 1 像素”的定义，边界距离直接作为 CD。

---

# 二、Profile 构建流程

## 1. 单柱轮廓提取

对每根柱逐行提取：

```text
(yᵢ, Lⱼ(yᵢ), Rⱼ(yᵢ))
```

每一行的宽度为：

```text
Wⱼ(yᵢ) = Rⱼ(yᵢ) − Lⱼ(yᵢ)
```

边缘坐标优先使用 Scharr 梯度、动态规划和亚像素定位结果；如果某行梯度证据不足，则几何轮廓可以使用 Binary 回退位置，但该行不作为 LER/LWR 的有效粗糙度点。

## 2. 有效柱筛选

只有满足以下条件的柱才进入平均 Profile：

- 没有被图像或 ROI 边界截断；
- 左右边缘有效点比例足够；
- `edge_support ≥ 0.6`；
- 柱体高度、宽度和边缘证据满足识别条件。

边缘支持度定义为：

```text
edge_support = (左侧 valid 点数 + 右侧 valid 点数) / (2 × 轮廓总行数)
```

整图自动识别时，最左和最右柱在 Profile 平均前已经排除；ROI 模式不执行整图两端排除规则。

---

# 三、Profile 高度归一化

不同柱子的顶部和底部可能不在同一像素位置，因此先把每根柱转换到相对高度坐标。

第 j 根柱的顶部和底部为：

```text
ytop,j = 柱顶坐标
ybottom,j = 柱底坐标
Hj = ybottom,j − ytop,j
```

相对高度定义为：

```text
z = (y − ytop,j) / Hj
```

因此：

```text
z = 0，表示柱顶
z = 1，表示柱底
```

### 参数含义

| 参数 | 含义 |
|---|---|
| `ytop,j` | 第 j 根柱顶部坐标 |
| `ybottom,j` | 第 j 根柱底部坐标 |
| `Hj` | 第 j 根柱实际高度 |
| `z` | 将不同高度柱子映射到同一相对高度 |

注意：高度被归一化，但宽度不归一化。也就是说，相同的 `z=0.5` 表示柱子中部，但不同样本的 CD 仍保持真实 nm 尺度。

---

# 四、中心对齐

如果不同柱子有轻微横向偏移，直接平均会使轮廓变宽。因此每根柱要先对齐到中线。

每个高度位置的柱中心为：

```text
Cⱼ(z) = [Lⱼ(z) + Rⱼ(z)] / 2
```

中心对齐后的左右坐标为：

```text
Lⱼ,aligned(z) = Lⱼ(z) − Cⱼ(z)
```

```text
Rⱼ,aligned(z) = Rⱼ(z) − Cⱼ(z)
```

对齐以后，柱子的中心位置为：

```text
Cⱼ,aligned(z) = 0
```

### 参数含义

| 参数 | 含义 |
|---|---|
| `Cⱼ(z)` | 第 j 根柱在高度 z 的中心位置 |
| `Lⱼ,aligned` | 中心对齐后的左边缘 |
| `Rⱼ,aligned` | 中心对齐后的右边缘 |

程序以柱子的中段中心作为主要横向对齐参考。这样可以把整体横向平移与真实形貌变化区分开。

---

# 五、统一 Profile 插值

为了让不同柱子可以在相同高度比较，程序建立统一的 401 个相对高度采样点：

```text
zk = k / 400， k = 0,1,2,...,400
```

因此：

```text
z0 = 0
z400 = 1
```

对每根柱的左边缘、右边缘和宽度进行插值：

```text
Lⱼ,k = interpolate(Lⱼ,aligned, zⱼ, zk)
```

```text
Rⱼ,k = interpolate(Rⱼ,aligned, zⱼ, zk)
```

```text
Wⱼ,k = Rⱼ,k − Lⱼ,k
```

### 参数含义

| 参数 | 当前值 | 含义 |
|---|---:|---|
| `PROFILE_SAMPLES` | 401 | 每根柱统一 Profile 的采样点数 |
| `k` | 0–400 | 统一高度点编号 |
| `zk` | `k/400` | 归一化高度 |

401 点的作用是把不同原始行数的柱子转换到相同高度分辨率，而不是增加真实图像信息。

---

# 六、平均 Profile

对每个统一高度点，所有有效柱等权平均。

平均左边缘：

```text
L̄k = (1/M) × Σj Lj,k
```

平均右边缘：

```text
R̄k = (1/M) × Σj Rj,k
```

平均 Profile 宽度：

```text
CD̄k = R̄k − L̄k
```

平均高度：

```text
H̄ = (1/M) × Σj Hj
```

平均 Profile 的实际纵向坐标：

```text
Ȳk = zk × H̄
```

### 重要区别

程序先平均左右边缘，再相减：

```text
CD̄k = R̄k − L̄k
```

这与直接把所有柱宽平均在数值上相近，但前者可以保留左右轮廓和柱间离散信息。

### 柱间离散

左边缘标准差：

```text
σL,k = sqrt(Σj(Lj,k − L̄k)² / M)
```

右边缘标准差：

```text
σR,k = sqrt(Σj(Rj,k − R̄k)² / M)
```

单样本界面中的阴影通常表示：

```text
L̄k ± σL,k
R̄k ± σR,k
```

阴影表示柱间形状离散，不是置信区间，也不是 LER。

---

# 七、CD 随高度变化曲线

中间图使用：

```text
横轴：CD̄k
纵轴：100 × zk
```

也就是：

```text
ProfileCD(z) = CD̄(z)
```

其中：

```text
0% = 柱顶
100% = 柱底
```

### 曲线判断

| 曲线变化 | 可能含义 |
|---|---|
| 顶部 0–18% 变宽 | T-top 增大 |
| 顶部逐渐变窄并呈圆滑过渡 | Top-rounding 增大 |
| 中部 CD 低于上下肩部趋势 | 中间细 |
| 中部 CD 高于上下肩部趋势 | 中间粗 |
| 底部 82–100% 变宽 | Footing 增大 |
| 全高度整体向右 | 整体 CD 增大 |
| 全高度整体向左 | 整体 CD 减小 |
| 左右轮廓变化不对称 | 可能存在倾斜、偏移或左右边缘识别差异 |

---

# 八、参考 CD 曲线和形貌判定

参考曲线不是固定方波，而是从相对稳定的柱身区域拟合得到。

在参考区间 `[a,b]` 内，对宽度进行最小二乘线性拟合：

```text
F[a,b](z) = α + βz
```

拟合目标：

```text
minα,β Σi[W(zi) − (α + βzi)]²
```

线性回归参数：

```text
β = Σi(zi − z̄)(Wi − W̄) / Σi(zi − z̄)²
```

```text
α = W̄ − βz̄
```

其中：

- `W̄`：参考区间宽度平均值；
- `z̄`：参考区间高度平均值；
- `α`：拟合截距；
- `β`：柱身 CD 随高度的趋势斜率。

如果参考区间有效点少于 2 个，程序回退使用实际宽度曲线，避免产生无意义的拟合结果。

---

## 1. T-top

参考区间：

```text
0.20 ≤ z ≤ 0.35
```

顶部检测区间：

```text
z ≤ 0.18
```

顶部正偏差：

```text
DT(z) = W(z) − F[0.20,0.35](z)
```

T-top：

```text
T-top = max(0, Q90({DT(z) | z≤0.18}))
```

判断：

- `DT(z)>0`：顶部比正常柱身趋势更宽；
- 数值越大，顶部外扩越明显；
- `max(0,...)` 确保没有外扩时不输出负的 T-top。

---

## 2. Top-rounding

参考区间：

```text
0.15 ≤ z ≤ 0.35
```

顶部区域：

```text
z ≤ 0.20
```

宽度缺失量：

```text
Di = max(0, F[0.15,0.35](zi) − W(zi))
```

顶部缺失面积，采用梯形积分：

```text
A = Σi(Di + Di+1)(Yi+1 − Yi) / 2
```

等效圆角半径：

```text
req = sqrt(A / (2 × (1 − π/4)))
```

其中：

- `A`：顶部相对于参考轮廓缺少的面积；
- `Yi`：实际 nm 高度坐标；
- `req`：面积等效圆角半径。

注意：`req` 不是直接拟合出来的真实圆弧半径，而是将缺失面积换算成圆角面积对应的等效半径。

---

## 3. 中间细

上肩部：

```text
0.20 ≤ z ≤ 0.30
```

下肩部：

```text
0.70 ≤ z ≤ 0.80
```

中部检测区域：

```text
0.35 ≤ z ≤ 0.65
```

计算上、下肩部平均宽度：

```text
WA = mean(W(z), 0.20≤z≤0.30)
```

```text
WB = mean(W(z), 0.70≤z≤0.80)
```

建立肩部连线：

```text
S(z) = WA + (WB − WA)(z − zA)/(zB − zA)
```

中间细：

```text
MiddleThin = max(0, Q90({S(z) − W(z) | 0.35≤z≤0.65}))
```

如果 `S(z)−W(z)>0`，说明中部实际宽度低于上下肩部趋势。

---

## 4. 中间粗

使用同一个肩部参考线 `S(z)`：

```text
MiddleThick = max(0, Q90({W(z) − S(z) | 0.35≤z≤0.65}))
```

如果 `W(z)−S(z)>0`，说明中部比上下肩部趋势更宽。

中间细和中间粗分别计算，不会相互抵消。

---

## 5. Footing

参考区间：

```text
0.65 ≤ z ≤ 0.80
```

底部检测区间：

```text
z ≥ 0.82
```

底部正偏差：

```text
DF(z) = W(z) − F[0.65,0.80](z)
```

Footing：

```text
Footing = max(0, Q90({DF(z) | z≥0.82}))
```

数值越大，表示柱脚相对于主体趋势向两侧展开越明显。

---

# 九、LER 与 LWR Profile 粗糙度

## 1. 有效高度范围

粗糙度只使用柱主体区域：

```text
0.15 ≤ z ≤ 0.85
```

左 LER 只使用左边缘有效点；右 LER 只使用右边缘有效点；LWR 需要同一高度处左右边缘都有效。

有效点条件：

```text
有效点数 ≥ 8
```

并且：

```text
有效点数 ≥ 主体点数 × 0.70
```

否则对应粗糙度为空。

## 2. 去趋势

对左边缘、右边缘或宽度进行一次线性拟合：

```text
v(Y) = a + bY
```

残差：

```text
e(Yi) = v(Yi) − (a + bYi)
```

残差标准差：

```text
σ(e) = sqrt(Σi(ei − ē)² / n)
```

## 3. 左右 LER

```text
LERL = 3 × σ(eL)
```

```text
LERR = 3 × σ(eR)
```

平均 LER：

```text
LER = (LERL + LERR) / 2
```

## 4. LWR

宽度：

```text
W(Y) = R(Y) − L(Y)
```

去趋势残差：

```text
eW(Yi) = W(Yi) − (aW + bWYi)
```

LWR：

```text
LWR = 3 × σ(eW)
```

### LER 与 LWR 的区别

- LER：边缘位置的波动；
- LWR：CD 宽度的波动；
- LWR 不等于左 LER 加右 LER；
- 左右边缘同步移动时，LER 可能增加，但 LWR 不一定增加；
- 左右边缘反向移动时，LWR 可能明显增加。

---

# 十、Baseline 对比

## 1. 统一数据

Baseline 和样本都使用相同的 Profile 流程：

1. 单柱提取；
2. 顶部和底部归一化；
3. 横向中心对齐；
4. 插值到 401 个高度点；
5. 对有效柱计算平均轮廓；
6. 使用相同的形貌公式计算指标。

## 2. 指标差值

对任意指标 `M`：

```text
ΔM = Msample − Mbaseline
```

解释：

| 差值 | 含义 |
|---|---|
| `ΔM>0` | 样本比 Baseline 增大 |
| `ΔM<0` | 样本比 Baseline 减小 |
| `ΔM=0` | 样本与 Baseline 相同 |

## 3. 变化率

```text
变化率 = 100 × ΔM / |Mbaseline|
```

当 `Mbaseline=0` 时，变化率留空，只显示绝对差值。

例如：

```text
Baseline LER = 2 nm
Sample LER = 3 nm
ΔLER = +1 nm
变化率 = 100 × 1 / 2 = +50%
```

如果：

```text
Baseline LER = 0 nm
Sample LER = 3 nm
```

则显示：

```text
ΔLER = +3 nm
变化率 = 空
```

---

# 十一、三种 Profile 对比方法

## 1. 平均轮廓图

左图显示：

- Baseline 的左右平均边缘；
- 样本的左右平均边缘；
- 单样本筛选时的柱间 ±1σ 阴影。

判断重点：

- 两侧同时向外：整体变宽；
- 两侧同时向内：整体变窄；
- 只有一侧明显移动：可能存在倾斜或偏移；
- 顶部外扩：T-top；
- 顶部圆滑收缩：Top-rounding；
- 中部向内：中间细；
- 中部向外：中间粗；
- 底部向外：Footing。

## 2. CD 随高度曲线

比较：

```text
ΔCD(z) = CDsample(z) − CDbaseline(z)
```

判断：

- `ΔCD(z)>0`：该高度样本更宽；
- `ΔCD(z)<0`：该高度样本更窄；
- 顶部区域变化：顶部形貌；
- 中部区域变化：腰部形貌；
- 底部区域变化：柱脚形貌。

## 3. 形貌指标表和点柱图

右图显示：

```text
T-top
Top-rounding
MiddleThin
MiddleThick
Footing
LER
LWR
```

点柱图：

- 向右为增加；
- 向左为减少；
- 点旁显示样本编号和 Δ 值。

表格图：

- 每列一个样本；
- 红色代表增加；
- 蓝色代表减少；
- 白色接近 0；
- 单元格内显示具体差值。

---

# 十二、Profile 判断示例

## 示例 1：顶部变宽

如果：

```text
ΔCD(z) > 0，且主要发生在 z=0–0.18
```

同时：

```text
ΔT-top > 0
```

通常判断为顶部 T-top 增加。

## 示例 2：顶部变圆

如果顶部 CD 没有突然外扩，而是从顶部向柱身逐渐平滑变窄，并且：

```text
ΔTop-rounding > 0
```

通常判断为顶部圆角增强。

## 示例 3：中间变细

如果：

```text
ΔCD(z) < 0，主要发生在 z=0.35–0.65
```

并且：

```text
ΔMiddleThin > 0
```

通常判断为中间细增强。

## 示例 4：底部变宽

如果：

```text
ΔCD(z) > 0，主要发生在 z=0.82–1.00
```

并且：

```text
ΔFooting > 0
```

通常判断为 Footing 增加。

## 示例 5：形貌基本不变但边缘更粗糙

如果平均 Profile 基本重合，但：

```text
ΔLER > 0 或 ΔLWR > 0
```

则主要变化是边缘粗糙度或线宽起伏增加，而不是主体形貌改变。

---

# 十三、结果可信度和注意事项

1. Profile 是相对高度对齐后的平均轮廓，适合比较形貌趋势。
2. 高度对齐不会把宽度归一化，CD 仍保留真实 nm 尺度。
3. 平均轮廓阴影是柱间离散，不是测量误差置信区间。
4. LER/LWR 采用逐柱原始有效点统计，避免多根柱平均后互相抵消。
5. 参考 CD 由稳定柱身拟合得到，不等于固定理想方波。
6. 第 90 百分位可以降低单个异常点对形貌量值的影响。
7. 柱底触及边界、柱底融入基底或严重过曝时，必须查看 Step7、Step9 和 Step10。
8. Profile 不能单独判断所有物理成因，最终应结合原图和过程图。
9. 比较不同图片时，必须分别确认每张图的 nm/px 比例尺。
10. 不同样本中的 `PR1` 只表示各自图像中的第一根保留柱，不表示同一物理柱。

---

# 十四、建议的人工判断顺序

1. 先看平均轮廓图，确认整体左右边缘是否重合。
2. 再看 CD 随高度曲线，定位差异发生的高度区间。
3. 计算并查看 `ΔCD(z)` 的正负。
4. 查看 T-top、Top-rounding、中间细、中间粗和 Footing 的差值。
5. 最后查看 LER 和 LWR，判断是否主要是粗糙度变化。
6. 如果指标和轮廓图不一致，回看 Step7 精定位点以及 Step9/Step10 灰度曲线。

Profile 的正确使用方式是“曲线定位变化区域，形貌公式进行分类，LER/LWR 判断边缘噪声”，三者需要一起使用。
