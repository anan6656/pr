# PR 柱体图像识别算法说明

## 文档用途

本文说明 PR Analyzer v6.0.0 自动识别流程中 step1–step10 的功能、公式、参数、参数含义和人工核查方法。公式均采用可复制到 Word 公式编辑器的线性格式。

核心流程：

```text
原图 → 灰度转换 → 保边滤波 → 局部增强 → 多候选 Binary
→ 分柱与 trench 排除 → Scharr 梯度精定位 → 动态规划
→ 亚像素定位 → 最终轮廓 → 灰度曲线核查
```

---

## 一、统一符号

| 符号 | 含义 | 单位 |
|---|---|---|
| `I(y,x)` | 原始图像像素 | 原始灰度或 RGB |
| `G(y,x)` | 8 位原始灰度图 | 0–255 |
| `F(y,x)` | 保边滤波图 | 0–255 |
| `E(y,x)` | CLAHE 增强图 | 0–255 |
| `B(y,x)` | Binary 二值图 | 0 或 1 |
| `x` | 横向坐标 | px |
| `y` | 纵向坐标 | px |
| `L(y)` | 左边缘坐标 | px 或 nm |
| `R(y)` | 右边缘坐标 | px 或 nm |
| `W(y)` | 柱宽，`W(y)=R(y)-L(y)` | px 或 nm |
| `z` | 归一化高度，顶部为 0、底部为 1 | 无量纲 |
| `Qp` | 第 p 百分位数 | 与输入相同 |
| `clip(a,l,h)` | 将 a 限制在 `[l,h]` | 与输入相同 |
| `σ` | 标准差 | 与输入相同 |
| `⊕` | 二值膨胀 | — |
| `⊖` | 二值腐蚀 | — |

---

# 二、逐步识别算法

## Step1 原始灰度转换

### 功能

把彩色图、高位深图统一转换成 8 位灰度图，为滤波、Binary 和梯度计算提供统一输入。

### 公式

彩色 BGR 图像：

```text
G0 = 0.114B + 0.587G + 0.299R
```

高位深图像：

```text
lo = Q1(I)
hi = Q99(I)
G = 255 × clip((I − lo)/(hi − lo), 0, 1)
```

当 `hi ≤ lo` 时，程序改用图像最小值和最大值；如果仍然相等，整幅图设为 0。

### 参数含义

| 参数 | 含义 |
|---|---|
| `0.114` | 蓝色通道灰度权重 |
| `0.587` | 绿色通道灰度权重 |
| `0.299` | 红色通道灰度权重 |
| `Q1` | 第 1 百分位，用于估计暗端背景 |
| `Q99` | 第 99 百分位，用于估计亮端上限 |
| `255` | 8 位灰度最大值 |

### 输出

`step1_gray.png`，以及后续步骤使用的 8 位灰度矩阵。

### 核查重点

确认柱体和背景的真实明暗关系，检查是否存在过曝、遮挡、文字、标尺或错误框选区域。

---

## Step2 保边滤波

### 功能

去除孤立噪声和小颗粒，同时保护柱体与背景之间的边缘。

### 公式

先做 3×3 中值滤波：

```text
M(p) = median{G(q) | q ∈ N3×3(p)}
```

再做双边滤波：

```text
F(p) = Σq w(p,q)M(q) / Σq w(p,q)
```

```text
w(p,q) = exp(−||p−q||²/(2σs²)) × exp(−(M(p)−M(q))²/(2σr²))
```

### 参数含义

| 参数 | 当前值 | 含义 |
|---|---:|---|
| `N3×3` | 3×3 | 中值滤波邻域 |
| `σs` | 7 | 空间距离权重，越大平滑范围越大 |
| `σr` | 20 | 灰度差权重，越大越容易跨过灰度边缘平滑 |
| OpenCV 邻域直径 | 5 | 双边滤波计算邻域 |

### 输出

`step2_filtered.png`。

### 注意

小于滤波尺度的真实纹理可能被削弱；最终边缘位置使用滤波图，但人工核查仍保留原灰度图。

---

## Step3 CLAHE 局部对比度增强

### 功能

增强低对比度区域，为 Binary 分割提供第二种候选图，特别适用于柱体和背景亮度不均的图像。

### 公式

局部直方图均衡映射：

```text
E(v) = 255 × CDF(v)
```

其中 `CDF(v)` 是当前局部区域内灰度 `v` 的累计分布函数。

### 参数含义

| 参数 | 当前值 | 含义 |
|---|---:|---|
| 分块数量 | 8×8 | 将图像分为局部区域 |
| `clipLimit` | 1.8 | 直方图限幅强度 |
| 输出范围 | 0–255 | 增强后的 8 位灰度范围 |

### 输出

`step3_enhanced.png`。

### 注意

CLAHE 图用于粗定位，不直接替代原始灰度进行最终尺寸测量。暗柱候选先反相，再执行增强。

---

## Step4 多候选 Binary 分割

### 功能

从滤波图、增强图和光照校正图中生成多组 Binary，并自动选择最可信的候选，而不是依赖一个固定阈值。

### 4.1 亮暗极性

```text
s = +1，亮柱
s = −1，暗柱
```

暗柱先反相：

```text
Idark = 255 − I
```

### 4.2 横向光照校正

```text
μx = meany F(y,x)
```

```text
x̃ = x − mean(x)
```

```text
b = Σx(μx − μ̄)x̃ / Σx x̃²
```

```text
Fcorr(y,x) = F(y,x) − b x̃
```

| 参数 | 含义 |
|---|---|
| `μx` | 位置 x 的纵向平均灰度 |
| `μ̄` | 所有 `μx` 的平均值 |
| `b` | 横向一阶亮度斜率 |
| `x̃` | 中心化横坐标 |

### 4.3 Otsu 阈值

对于阈值 `t`：

```text
C0 = {I ≤ t}
C1 = {I > t}
```

类间方差：

```text
σb²(t) = ω0(t)ω1(t)[μ0(t) − μ1(t)]²
```

最佳阈值：

```text
t* = argmax_t σb²(t)
```

Binary：

```text
B(y,x) = 1，当 I(y,x) > t*
B(y,x) = 0，当 I(y,x) ≤ t*
```

| 参数 | 含义 |
|---|---|
| `ω0,ω1` | 两类像素比例 |
| `μ0,μ1` | 两类像素平均灰度 |
| `t*` | 使类间差异最大的阈值 |

### 4.4 Binary 闭运算

```text
Bclose = (B ⊕ K) ⊖ K
```

| 参数 | 当前值 | 含义 |
|---|---:|---|
| `K` | 3×3 | 形态学结构元素 |
| 膨胀 | 1 次 | 连接小裂缝、扩大前景 |
| 腐蚀 | 1 次 | 恢复主体边界、填补孔洞 |

### 输出

`step4_binary.png`，显示最终选中的 Binary 候选。

---

## Step5 Binary 描边、分柱和 trench 排除

### 功能

估计柱体主体高度，分离相邻柱子，删除碎片，并通过柱内外灰度关系降低 trench 误识别。

### 5.1 行对比度

每一行的稳健对比度：

```text
C(y) = Q90(G(y,:)) − Q10(G(y,:))
```

随后使用高斯 11 点平滑：

```text
C̃(y) = Gaussian11(C(y))
```

### 5.2 行边缘活动度

先 Gaussian5 平滑，再计算横向 Sobel 梯度：

```text
Sx(y,x) = ∂G/∂x
```

每行取梯度绝对值第 85 百分位：

```text
E(y) = Q85(|Sx(y,:)|)
```

再平滑：

```text
Ē(y) = Gaussian11(E(y))
```

### 5.3 综合活动度

```text
Ac(y) = clip(C̃(y)/Q95(C̃), 0, 2)
```

```text
Ae(y) = clip(Ē(y)/Q95(Ē), 0, 2)
```

```text
A(y) = 0.72Ac(y) + 0.28Ae(y)
```

| 参数 | 当前值 | 含义 |
|---|---:|---|
| `Q10,Q90` | 10%、90% | 估计一行的稳健灰度范围 |
| `Gaussian11` | 11 点 | 平滑主体活动度 |
| `Gaussian5` | 5×5 | 平滑 Sobel 输入 |
| `Q85` | 85% | 提取一行较强边缘能量 |
| `Q95` | 95% | 归一化活动度上限 |
| `0.72` | 72% | 灰度对比权重 |
| `0.28` | 28% | 边缘活动权重 |
| `clip(...,0,2)` | 0–2 | 防止异常峰值支配结果 |

### 5.4 主体行范围

将活动度归一化为 0–255 后再次使用 Otsu。主体起始门槛为：

```text
start_ratio = max(0.22, clip(0.75 × otsu_ratio, 0.12, 0.65))
```

主体行判断：

```text
A(y) ≥ start_ratio × max(A)
```

| 参数 | 含义 |
|---|---|
| `0.22` | 最低主体活动度比例 |
| `0.75` | Otsu 门槛折减系数 |
| `0.12` | 自适应门槛下限 |
| `0.65` | 自适应门槛上限 |
| `0.55×图高` | 整图模式优先从上方区域寻找主体 |
| `0.04×图高` | 连续主体段最小长度比例 |

### 5.5 柱种子和 cell

主体上下各裁去约 1/6，得到种子带：

```text
pseed(x) = Gaussian5(meany B(y,x))
```

种子条件：

```text
pseed(x) ≥ 0.5
```

并且连续宽度不少于：

```text
5 px
```

相邻种子中心的中点作为 cell 分界。每根柱只在自己的 cell 内寻找边缘。

### 5.6 柱候选过滤

| 条件 | 当前要求 |
|---|---:|
| 有效主体行 | 至少 20 行 |
| 普通柱中位宽 | 至少 15 px |
| 贴边柱中位宽 | 至少 3 px |
| 高宽比 | 柱高 / 中位宽 ≥ 1.2 |
| 单行连续主体 | 至少 3 px |
| 相邻有效行断裂 | 大于 8 px 时分段 |

### 5.7 trench 极性判定

```text
dy = s × [median(Ginside) − median(Goutside)]
```

噪声估计：

```text
η = 1.4826 × median(|G − median5(G)|)
```

接受条件：

```text
median(dy) > max(1.5, 1.8η)
```

且：

```text
至少 65% 的采样行满足 dy > 1
```

| 参数 | 含义 |
|---|---|
| `Ginside` | 柱中心约一半区域的灰度 |
| `Goutside` | 柱两侧距边界约 2–12 px 的背景灰度 |
| `1.4826` | 将 MAD 换算为近似标准差的稳健系数 |
| `1.5` | 最低灰度差门槛 |
| `1.8η` | 随图像噪声变化的门槛 |
| `65%` | 通过极性检查的最小采样行比例 |

### 5.8 候选总分

```text
S = Ccount + 1.45Fwidth + 1.20Fconsistency + 0.85Fheight + 0.55Fgap + 0.45Fedge + Cgray − Pedge
```

各项：

```text
Ccount = min(3, 0.55 × Σj coveragej²)
```

```text
Fwidth = meanj exp(−|wj − wtarget| / max(1,tolerance))
```

```text
Fconsistency = exp(−max(MAD(w)/max(2,0.22×median(w)), std(w)/max(2,0.18×median(w))))
```

```text
Fheight = clip(median(柱行数)/图像高度, 0, 1)
```

```text
Fgap = clip(median(中心间距)/max(3,0.8×median(柱宽)), 0, 1)
```

```text
Fedge = clip(median(左右边缘梯度)/8, 0, 1)
```

```text
Cgray = 0.35 × clip((Ginside − Ggap)/15, −1, 1)
```

```text
Pedge = 0.25 × 贴边柱数量
```

| 参数 | 含义 |
|---|---|
| `wj` | 第 j 根候选柱宽度 |
| `wtarget` | 用户目标 CD；自动模式时不强制使用 |
| `tolerance` | 用户允许 CD 半宽误差 |
| `MAD` | 中位绝对偏差，衡量宽度离散 |
| `coveragej` | 候选柱覆盖主体高度的比例 |
| `Ggap` | 相邻柱间隙灰度 |
| `Pedge` | 贴近图像边缘的惩罚 |

三路 Binary 均失败时，程序才启用几何灰度提议。该候选仍然必须通过极性、宽度和高度检查，并额外扣分 `0.25`。

### 输出

`step5_binary_contours.png`。橙色为粗轮廓，此时尚未执行整图两端柱排除。

---

## Step6 整图两端柱排除

### 功能

按照软件约定，整图自动识别强制排除最左和最右柱，避免边界残柱影响统计。

### 公式

```text
xc,j = (xL,j + xR,j) / 2
```

按中心坐标排序：

```text
xc,1 < xc,2 < ... < xc,n
```

整图模式保留：

```text
j = 2,3,...,n−1
```

如果 `n≤2`，则全部排除。ROI 模式不执行此规则。

### 输出

`step6_outermost_exclusion.png`。红色为排除柱，绿色为保留柱。

---

## Step7 Scharr 梯度、动态规划和亚像素定位

### 功能

在 Binary 给出的粗范围内寻找更精确的左右、顶部和底部边缘，并保证相邻行的边缘连续。

### 7.1 搜索半径

```text
r = min(32, max(3, floor(0.32 × max(本柱中位宽,候选典型宽))))
```

| 参数 | 含义 |
|---|---|
| `32 px` | 最大搜索半径 |
| `3 px` | 最小搜索半径 |
| `0.32` | 柱宽到搜索半径的比例 |

### 7.2 Scharr 梯度

横向 Scharr 核：

```text
[−3   0   3]
[−10  0  10]
[−3   0   3]
```

纵向 Scharr 核：

```text
[−3  −10  −3]
[ 0    0   0]
[ 3   10   3]
```

```text
gx = s × Scharrx(F) / 32
```

```text
gy = s × Scharry(F) / 32
```

方向：

```text
左边缘：gx
右边缘：−gx
顶部：gy
底部：−gy
```

`32` 是 Scharr 响应归一化因子，不是像素尺寸。

### 7.3 动态规划单点代价

```text
G75 = max(1, Q75(每行搜索带内非负梯度最大值))
```

```text
Ui(k) = −clip(max(gi(k),0)/G75, 0, 3) + 0.15(offsetk/r)²
```

| 参数 | 含义 |
|---|---|
| `gi(k)` | 第 i 行第 k 个搜索点的梯度响应 |
| `G75` | 梯度响应第 75 百分位尺度 |
| `offsetk` | 候选点距 Binary 种子的位置 |
| `r` | 搜索半径 |
| `0.15` | 偏离 Binary 粗边界的惩罚权重 |
| `3` | 梯度奖励的最大截断值 |

### 7.4 相邻行连续性代价

```text
V(k,l) = 0.07 × min((xi,k − xi−1,l)², 36) + 0.10 × |xi,k − xi−1,l|
```

| 参数 | 含义 |
|---|---|
| `0.07` | 大跳跃平方惩罚权重 |
| `36` | 平方跳跃项的截断值，即 6² |
| `0.10` | 绝对跳跃惩罚权重 |
| `xi,k` | 当前行候选坐标 |
| `xi−1,l` | 上一行候选坐标 |

### 7.5 动态规划递推

```text
Di(k) = Ui(k) + minl[Di−1(l) + V(k,l)]
```

最后一行选代价最小点，再反向回溯得到整条边缘。

### 7.6 亚像素抛物线插值

设峰值附近三个梯度值为：

```text
a = g(x−1)
 b = g(x)
 c = g(x+1)
```

当 `b≥a`、`b≥c` 且 `a−2b+c<−10⁻⁶` 时：

```text
δ = 0.5 × (a−c)/(a−2b+c)
```

```text
xsubpixel = xpeak + clip(δ, −0.5, 0.5)
```

有效点条件：

```text
峰强度 > max(0.8, 0.10 × G75)
```

否则回退到 Binary 点并标记 `valid=0`。

### 7.7 顶部和底部

纵向搜索范围：

```text
y ∈ [ycoarse − min(r,8), ycoarse + min(r,8)]
```

有效峰数量要求：

```text
有效峰数量 ≥ max(2, floor(列数/3))
```

底部梯度太弱时，在柱外距离 3–9 px 范围检查基座参考边缘。至少 4 个参考点且：

```text
std(yreference) < max(2, 0.6r)
```

才使用 `base_reference`。

---

## Step8 最终闭合轮廓

### 功能

将左右边界、顶部和底部组合成最终闭合轮廓，并区分几何计算点与粗糙度有效点。

### 公式

```text
Contour = {(L(yi),yi)} ∪ {(R(yi),yi)}
```

如果某一行精定位失败：

- 几何轮廓使用 Binary 回退坐标，保持轮廓连续；
- LER/LWR 只使用 `valid=1` 的精定位点。

### 质量条件

| 条件 | 处理 |
|---|---|
| 边缘贴框比例 ≥ 10% | 相关点标为无效 |
| 左右边缘交叉 | 回退到 Binary 种子 |
| 左右间距 ≤ 1 px | 回退到 Binary 种子 |
| 顶部/底部触及图像边界 | 标记可能截断 |

### 输出

`step8_final_contours.png`、points CSV 和 manifest JSON。

---

## Step9 逐行灰度曲线

### 功能

验证每一行的左右定位点是否位于真实灰度边缘。

### 曲线

```text
原灰度曲线：G(y,x)
滤波曲线：F(y,x)
```

横坐标为 `x`，纵坐标为灰度值。左右点应靠近灰度跃迁，而不是落在柱内平台、背景噪声或邻柱边缘。

### 参数

| 参数 | 当前值 | 含义 |
|---|---:|---|
| 扩展半径 | `r` | 柱边界外扩展的曲线显示范围 |
| 每页曲线数 | 24 | 6×4 排列 |
| x 范围 | 左右极值 ± r | 保留边缘附近背景 |

### 输出

`step9_*` 分页图。黄色点表示弱证据或回退点。

---

## Step10 逐列灰度曲线

### 功能

验证柱顶、柱底、基座界面和柱高。

### 曲线

```text
原灰度曲线：G(y,x)
滤波曲线：F(y,x)
```

横坐标为 `y`，纵坐标为灰度值。程序叠加顶部、底部、基座参考和 Binary 回退点的来源。

### 参数

| 参数 | 当前值 | 含义 |
|---|---:|---|
| y 范围 | 顶部/底部 ± r | 包含界面附近背景 |
| 每页曲线数 | 24 | 6×4 排列 |
| 列范围 | floor(最左边界) 到 ceil(最右边界) | 覆盖整个柱体 |

### 输出

`step10_*` 分页图，以及完整的 `PR*_profiles.npz` 灰度矩阵。

---

# 三、识别结果的有效性规则

## 1. 自动整图识别

自动整图识别强制排除最左和最右柱，包括残柱和完整柱。

## 2. ROI 识别

ROI 模式不排除 ROI 内最左和最右柱，但如果柱子触及 ROI 边界，仍可能被标记为截断，不能进入平均形貌。

## 3. 过程图输出

自动识别过程图写入：

```text
原图同名文件夹/test/
```

包括：

```text
step1_gray.png
step2_filtered.png
step3_enhanced.png
step4_binary.png
step5_binary_contours.png
step6_outermost_exclusion.png
step7_refined_points.png
step8_final_contours.png
step9_*.png
step10_*.png
manifest_*.json
points_*.csv
PR*_profiles.npz
```

## 4. 空结果

即使没有保留柱，程序仍保存 step1–step8 和空点表；没有可保留柱时不生成 step9/step10 曲线。

---

# 四、参数如何影响识别

| 参数类别 | 参数变大时的通常影响 |
|---|---|
| 滤波空间尺度 `σs` | 更平滑，但细小边缘可能被削弱 |
| 滤波灰度尺度 `σr` | 更容易跨灰度差异平滑，可能降低边缘对比 |
| CLAHE `clipLimit` | 对比度更强，但噪声和伪边缘也可能增强 |
| Binary 闭运算核 | 更容易连接裂缝，但可能连接相邻柱或基底 |
| 主体活动度门槛 | 提高后更严格，降低后更容易保留弱柱和噪声 |
| 柱宽容差 | 增大后允许更宽的候选，误检风险也可能增加 |
| Scharr 搜索半径 `r` | 增大后可修正更大偏差，但更容易追到错误边缘 |
| 动态规划连续性权重 | 增大后轮廓更平滑，但可能忽略局部真实形貌 |
| 峰强度门槛 | 提高后有效点更少但可信度更高 |
| 正偏差百分位数 | 越高越接近严重局部展宽，越低越接近整体典型展宽 |

---

# 五、人工核查顺序

1. 看 step1：确认原始明暗和框选区域。
2. 看 step2：确认滤波没有抹掉柱边。
3. 看 step3：确认增强没有制造明显伪纹理。
4. 看 step4：确认 Binary 覆盖柱身而不是覆盖 trench 或基底。
5. 看 step5：确认柱间 cell 和柱内外灰度关系正确。
6. 看 step6：确认最左和最右柱按规则排除。
7. 看 step7：确认精定位点落在灰度跃迁位置。
8. 看 step8：确认最终轮廓没有交叉、贴框或明显回退。
9. 看 step9：逐行核对左右边缘。
10. 看 step10：核对柱顶、柱底和基座界面。

最终应同时查看原图、过程图、points CSV 和 manifest JSON，不应只依据单个形貌数值判断。
