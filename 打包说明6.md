# 单 PY 文件离线打包

只需携带 `打包源码/pr_4.2.py`。程序版本为 **v6.0.0**；识别、界面、帮助、Baseline 对比和打包配置全部内置，不需要 spec、脚本、说明文档或模型文件。

## 公司电脑打包

公司电脑须事先安装 Python，以及 NumPy>=2、OpenCV、Tkinter、Matplotlib、PyInstaller。在 PY 所在文件夹打开终端：

```powershell
python pr_4.2.py --build-exe
```

程序只使用本机环境，不会联网下载或安装任何内容。EXE 生成在这个 PY 所在目录的 `dist/PR_Analyzer_v6.0.0.exe`；打包完成后自动清理临时文件。

如本机使用现有 Miniconda 环境，在项目根目录运行：

```powershell
& 'D:\code\miniconda\python.exe' '.\打包源码\pr_4.2.py' --build-exe
```

## 公司电脑仅运行

可以直接携带已经打包好的 `dist/PR_Analyzer_v6.0.0.exe`，公司运行电脑不需要安装 Python，也不需要外网。选择公司电脑上的图片即可分析。

如果公司电脑没有打包依赖，一个 PY 文件不能凭空生成 EXE；需要事先准备匹配公司 Windows/Python 版本的离线安装包，或在已有环境的电脑上打包后带入 EXE。

`使用说明` 文件夹仅供阅读，不参与打包。过程图写入原图同名文件夹的 `test` 子文件夹；Baseline 报告写入 EXE 同级的 `baseline_comparison` 文件夹。
