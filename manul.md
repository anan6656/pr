# PR 柱子智能识别与测量工具

这是 `pr4_copy.py` 的快速使用说明。脚本可直接运行，也可以用 PyInstaller 打包成单文件程序。

## 最快上手

1. 将 `pr4_copy.py` 与 `.tif/.tiff` 图像放在同一文件夹。
2. 运行 `python pr4_copy.py`。
3. 选择图像后，左侧只需要填写两个识别先验：
   - **估计 Line CD (px)**：完整柱宽的大致像素值；不确定时填 `0`，程序自动估计。
   - **允许误差 ±(px)**：允许的半宽范围，例如 `83 ± 5`。
4. 点击“分析选中图像”。识别置信度不足时会自动尝试亮/暗极性、增强、平滑、均衡化和局部对比度版本，并选出得分最高的候选。

左右边缘被裁掉的残柱会自动放宽筛选，不会因为宽度较小而被直接删除。状态栏会显示候选版本、置信度、尝试次数、有效 CD 范围和提示信息。

## 识别策略

- TIFF 先归一化为 8-bit 灰度，再进行局部对比度增强、降噪和 Sobel 灰度斜率计算。
- 横向亮度/结构投影定位柱区，纵向逐行追踪左右物理边界，生成 `W(y)`（CD 随高度变化）曲线。
- 自动重试会核验亮柱与暗柱两种极性；低置信度时再尝试增强候选。
- 所有候选统一经过 footing 台阶修剪、粘连提示和近柱错拆合并。
- ROI 识别与整图识别使用同一套流程；ROI 内部也保留增强和平滑候选。

## 灰度曲线与手动量测

中间的“灰度剖面”面板有三种模式：

- **直线剖面**：在原图选择 A、B，曲线实时显示 A→B 的灰度。
- **横向行剖面**：输入 `y`，查看该行整幅图的灰度排列。
- **纵向列剖面**：输入 `x`，查看该列从上到下的灰度变化。

曲线交互：

- 普通单击：在原图绿色十字 `P` 定位，并显示像素坐标和灰度。
- `Shift+单击` 或右键：依次设置橙色 `M1/M2` 两个量测端点。
- 拖动橙色端点：曲线区间、原图连线、距离（px/nm）和灰度差实时同步。
- 也可以直接拖动原图上的蓝色 A/B 端点；曲线会立即刷新。
- “清除量测”会同时清除曲线端点和原图量测线。

## Target 与 Judge

右侧两个统计块（自动 `_tool`、ROI `_user`）可以分别设置 Target：

- `Line CD`：目标线宽（nm）。
- `±nm` 与 `±%`：取两者换算结果中的较大者作为容差。
- `高度nm`：填 `0` 表示不判断高度。

Judge 是方向性比例，不再限制在 0～100：

```text
Judge = mean(Profile W(y)) / Target CD × 100 (%)
```

- `100`：Profile 与 Target 等宽；
- `>100`：Profile 比 Target 宽；
- `<100`：Profile 比 Target 窄。

修改 Target 后，表格、`*_stats_nm.txt` 和 `*_target_check.txt` 会同步更新。未启用 Target 时 Judge 显示 `-`。

## 输出文件

每张图默认输出到同名子文件夹：

- `*_tool_result.png` / `*_user_result.png`：识别预览图。
- `*_boundaries.txt`、`*_boundaries_nm.txt`：逐行边界。
- `*_stats_nm.txt`：高度、CD、角度、LER、Profile 和 Judge 统计。
- `*_target_check.txt`：Target 对比、PASS/FAIL、形貌和相似度明细。

文本文件末尾会附带本次识别的可追溯参数：

- 识别模式与候选版本（包括亮/暗极性）；
- 识别置信度与候选尝试次数；
- 用户输入的估计 Line CD/允许误差；
- 有效先验范围与实际观测 CD 中位数/范围。

## 自检与打包

```powershell
python pr4_copy.py --selftest
python -m py_compile pr4_copy.py
```

自检会优先使用同目录的 `pr3.tif`，若不存在则尝试 `pr2.tif`，结果写入 `selftest_ok.txt`。

打包示例：

```powershell
PyInstaller --onefile --windowed pr4_copy.py
```
